# Backend (.env)
# Copia este archivo como: backend/.env

# ===== CONFIGURACIÓN DE SUPABASE =====
# Obtén estos valores desde tu proyecto de Supabase:
# Settings > API

SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave pública (anon) para operaciones normales
SUPABASE_ANON_KEY=tu-anon-key-aqui

# Service Role Key (requerida para scripts/admin: bootstrap, migraciones, etc.)
SUPABASE_SERVICE_KEY=tu-service-role-key-aqui

# Opcional: timeout de fetch para Supabase (ms)
SUPABASE_FETCH_TIMEOUT_MS=20000

# ===== CONFIGURACIÓN DEL SERVIDOR =====
HOST=127.0.0.1
PORT=5000

# ===== BOOTSTRAP ADMIN (opcional) =====
# Se usa en: npm run bootstrap-admin
BOOTSTRAP_ADMIN_EMAIL=admin@tu-dominio.com
BOOTSTRAP_ADMIN_PASSWORD=cambia-esto
BOOTSTRAP_ADMIN_NOMBRE=Admin
BOOTSTRAP_ADMIN_TELEFONO=
